---
title: 개발 가이드
tags: [board, development, setup]
status: current
last-reviewed: 2026-08-01
---

# 개발 가이드

## 사전 요구사항

- JDK 21
- MySQL, 로컬 DB `board`
- OAuth를 사용할 경우 Kakao/Google client 설정

## 설정

애플리케이션은 `.env`를 선택적으로 properties 형식으로 읽는다. 저장소의 실제 비밀값을 문서나 커밋에 포함하지 않는다.

필요 환경 변수:

```properties
KAKAO_REST_API=...
KAKAO_SECRET=...
KAKAO_CALLBACK=...
GOOGLE_CLIENT_ID=...
GOOGLE_CLIENT_SECRET=...
APP_UPLOAD_DIR=./uploads
```

현재 `application.yaml`의 DB 연결은 `localhost:3306/board`, 사용자 `root`로 고정되어 있다. 개발자별 설정과 운영 배포를 위해 환경 변수화하는 것이 필요하다.

## 실행과 검증

```powershell
.\gradlew.bat bootRun
.\gradlew.bat test
```

- 서버 포트: `8090`
- 이미지 디렉터리: 기본 `./uploads`
- 스키마: Hibernate `ddl-auto: update`
- 2026-08-01 기준 `gradlew.bat test` 성공. 테스트는 Spring context 로딩 1개뿐이므로 기능 회귀를 보장하지 않는다.

## 변경 시 문서 갱신 규칙

| 코드 변경 | 함께 갱신할 문서 |
|---|---|
| Controller path/method | [[05-API-레퍼런스]] |
| DTO 필드/validation | [[05-API-레퍼런스]], 필요 시 [[07-프런트엔드-요구사항]] |
| Entity/비즈니스 규칙 | [[03-도메인-모델]] |
| Security/OAuth/cookie | [[04-인증과-권한]] |
| 새 기능/범위 | [[01-프로젝트-개요]], [[07-프런트엔드-요구사항]] |
| 임시 제약/불일치 | [[08-결정사항과-알려진-이슈]] |

## 권장 최소 테스트

- 회원가입 중복, USER의 ADMIN 승격 차단
- 로그인/재발급/로그아웃 cookie 흐름
- 공개 GET과 보호된 쓰기 API의 401/403
- 작성자만 게시글·댓글 수정/삭제
- 이미지 타입·개수·크기 제한과 실패 시 정리
- 댓글 깊이/삭제/게시글 불일치 규칙
- 반응 생성/취소/전환과 집계
- 자기 활동 제외 알림, 소유자 읽음 처리

